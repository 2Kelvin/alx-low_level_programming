#include "main.h"

/**
  * _isupper - checks for uppercase letters
  * Return: 1 if uppercase, 0 if not
  */

int _isupper(int c)
{
	int i;

	for (i = 'A'; i <= 'Z'; i++)
		if (c == l)
			return (1);
		else
			return (0);
}
