#include "main.h"

/**
  * infinite_add - adds two numbers from strings
  * @n1: parameter number one
  * @n2: parameter number two
  * @r: result
  * @size_r: buffer size
  * Return: r
  */

char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	*r = n1 + n2;
	return (r);

	return ('0');
}
